# Welcome to the network module
# This module provides the network base (VPC, subnet, internet gateway, security group) for the instances that will be deployed
# Security is purposefully lax, as these are intended to be isolated, temporary test instances